# Skill Changelog

## 2026-07-19 (adversarial bug-hunt run, post-task-18)
**Change:** Added a new guideline to CLAUDE.md (below the hard invariants) requiring "does this text mention X" checks to match X as a whole word (`\bX\b`), never as a raw substring (`String.includes`) — especially for short, generically-derived tokens — and requiring such a check's test suite to include a case proving a false pass is actually prevented (the token present only inside an unrelated word, with the real mention absent).

**Evidence:** PROGRESS.md, 2026-07-19 "adversarial bug-hunt run, post-task-18" entry — `reviewer/tests/docProseDrift.test.js`'s `mentionsSkill` (added in task 18, commit `5a31b92`) used raw substring `includes` per hyphen-split skill-name token. For skill `review-pr`, token `"pr"` substring-matches common words like `Express`/`approach`/`improve`, and token `"review"` substring-matches `"reviewer"` (this repo's own package name, present in nearly every description the test checks). Reproduced directly: stripped every literal `"review-pr"`/`"PR review"` mention from the real `README.md`, leaving unrelated `"Express"`/`"reviewer/"` text in place, and confirmed the real `mentionsSkill` function still returned `true` — a false pass on text that no longer mentions the skill at all, defeating the drift-detection task 18 was written to provide, in the very commit that added it. This is a new bug class — not any of the nine existing CLAUDE.md guidelines (aggregation-scope, string-literal masking, CLI flag-parsing, exemption-scope, precondition-before-side-effect, etc.) — so it's captured as its own entry. Fixed in the same run: switched `mentionsSkill` to a `\btoken\b` word-boundary regex per token; re-verified all 14 pre-existing `docProseDrift.test.js` cases still pass against the real repo's current prose (which uses "PR"/"review" as genuine whole words), and added one new regression case asserting the stripped-text false-pass scenario now correctly returns `false`. `reviewer/` suite 259/259 green (258 pre-existing + 1 new); `sample-app/` unaffected, 43/43 green.

## 2026-07-19 (adversarial bug-hunt run, post-task-17)
**Change:** Added a new guideline to CLAUDE.md (below the hard invariants) requiring a request handler that both validates a precondition and performs a persistent side effect to run every such precondition check before committing to the side effect, never after or interleaved between writes — and requiring the precondition-fails test case to assert the side-effect collaborator was never called, not just that the HTTP response looks right.

**Evidence:** PROGRESS.md, 2026-07-19 "adversarial bug-hunt run, post-task-17" entry — `sample-app/src/controllers/authController.js`'s `register` (unchanged by task 17 itself) called `User.create(...)` before `signToken(...)`, which task 17 (aa11f42) had just wired to call `resolveJwtSecret()` and throw in production when `JWT_SECRET` is unset or still the `.env.example` placeholder. Reproduced directly: with `NODE_ENV=production` and no `JWT_SECRET`, `POST /api/auth/register` still called `User.create` (persisting the account) before the throw, returning 500 with no token — a client told registration failed while the account silently exists, and every retry with the same email then gets 409 forever. Task 17's own regression test for this exact scenario mocked `User.create` to resolve and asserted only on the response, never on whether `User.create` was called, so it passed despite the account being created. This is a new bug class — not any of the eight existing CLAUDE.md guidelines (aggregation-scope, string-literal masking, CLI flag-parsing, exemption-scope, etc.) — so it's captured as its own entry. Fixed in the same run (moved `resolveJwtSecret()` to run immediately after input validation, before any DB call; added `expect(User.create).not.toHaveBeenCalled()` to the existing test, verified it fails against the pre-fix code and passes against the fix; `sample-app/` suite 43/43 green, unchanged count since this only strengthened an existing test).

## 2026-07-19 (adversarial bug-hunt run)
**Change:** Added a new guideline to CLAUDE.md (below the hard invariants) requiring any brace/paren depth-counting helper used to find a per-occurrence block/statement boundary to first mask out characters that fall inside string/template literals before counting — never count `{`/`}`/`(`/`)` raw — and requiring such a helper's test suite to include both an opening-shaped-character-in-a-string case and a closing-shaped-character-in-a-string case.

**Evidence:** PROGRESS.md, 2026-07-19 "adversarial bug-hunt run" entry — `reviewer/src/utils/collectBoundedBlock.js` (shared by `errorHandling.js`'s handler-block check and `performance.js`'s forEach-block check) and `errorHandling.js`'s own independent `collectThenStatement` both counted brace/paren characters character-by-character with no awareness that a character inside a string literal isn't real code structure — a bug class distinct from (and orthogonal to) the five-times-recurring per-occurrence-scoping class already documented in the guideline above it, since both helpers here already *were* correctly scoped per-occurrence (the fix task 15 extracted `collectBoundedBlock` specifically to consolidate). Reproduced directly, all four false-positive/false-negative shapes described in the new CLAUDE.md paragraph, none caught by any of the four pre-existing test files for these two helpers (none had a string-literal-containing case). Fixed with a new shared `reviewer/src/utils/maskStringLiterals.js`, used by both call sites. Suite went 227/227 → 237/237 green in `reviewer/` from this fix alone (10 net-new regression/unit tests); a separate CLI-parsing bug found in the same run (a value-taking flag silently consuming the next flag's name as its own value, e.g. `--out --repo <path>` dropping `--repo`'s real value with no error) was fixed across all four CLI parsers via a new shared `consumeFlagValue` helper, taking `reviewer/` on to 244/244; two `sample-app` bugs (a type-confused `req.body.email`/`password` crashing `authController`'s register/login into a 500 instead of 400, and a register-time unique-index race returning 500 instead of 409) were also fixed in the same run, plus three IDOR-shaped `tasks.test.js` assertions strengthened to actually check the `owner` query filter, taking `sample-app/` from 29/29 to 33/33. All four fixes are logged in full in PROGRESS.md; only the string-literal depth-counting bug is logged here, since it's the one CLAUDE.md/skill/review-rules/backlog change this run made (the other three are ordinary code + test fixes, not self-modifications under hard invariant 3).

## 2026-07-19 (self-audit round 2, backlog additions)
**Change:** Added TASKS.md items 17–18 (backlog order/content change): harden `sample-app`'s `JWT_SECRET || 'dev-secret'` fallback for production; add automated regression coverage against doc/prose drift inside this repo's own README.md and plugin manifests. No task marked DONE this run (self-audit and propose only, matching round 1's task-13 precedent).

**Evidence:** RETRO.md's "Retro round 2" section (this run) synthesizes evidence already on record in PROGRESS.md rather than introducing anything new: (17) the `JWT_SECRET || 'dev-secret'` fallback was raised as an open question in the post-task-14 PROGRESS.md entry, restated verbatim in the task-16 entry, and restated a third time in the post-task-16 entry, with no new information added across any of the three mentions and the fallback still present in both `authController.js` and `middleware/auth.js` as confirmed by re-grepping this run — exactly the "known, scoped, cheap follow-up nobody has decided against" pattern round 1's own RETRO.md "what to improve" section prescribed routing into TASKS.md rather than re-logging as an open question indefinitely, and this is the first run to actually apply that prescription. (18) The post-task-14 PROGRESS.md entry documents three separate instances of doc/prose drift found inside this repo's own README.md and `plugin/.claude-plugin/*.json` in a single hunt (a hardcoded, already-stale Jest test count; prose describing a shipped feature as not-yet-built; a marketplace description missing a just-added skill), all caught only by manual inspection, none by any automated test — a real, observed gap for a repo whose own product is a doc-drift detector for its users. Suite unaffected (docs/backlog-only change): `reviewer/` 227/227 green, `sample-app/` 29/29 green, both re-verified after this change.

## 2026-07-18 (adversarial bug-hunt run, post-task-16)
**Change:** Added a new guideline to CLAUDE.md (below the hard invariants) requiring a user-facing status/success message that describes which of several actions a collaborator took (e.g. "created" vs. "updated") to be derived from that collaborator's actual return value, never hardcoded for only one action — and requiring such a reporting caller's test suite to mock the collaborator with more than one distinguishable return shape, not just one fixed canned value.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-16" entry — `reviewer/src/reviewPrCli.js` printed a hardcoded `"Posted review comment to ..."` message regardless of whether the just-completed `postReviewComment` call actually created a new PR comment or PATCHed an existing one in place (task 16's own headline feature). `postReviewComment` correctly implements the create-vs-update decision internally but threw it away when building its return value (just the raw GitHub API response body), leaving the CLI with no signal to branch its message on. `reviewPrCli.test.js`'s entire success-path coverage mocked `postReviewComment` as `jest.fn().mockResolvedValue({ id: 1 })` — a single fixed return shape — so the hardcoded message matched trivially in every test and the gap shipped in the very commit that introduced the behavior it now misreports (a user or CI log reading "Posted" on the 2nd/3rd/Nth run against the same PR — the `review-pr` skill's own stated normal use case — would wrongly conclude a new comment was created each time). Fixed in the same run: `postReviewComment` now returns `{ ...comment, updated: Boolean(existing) }`; `reviewPrCli.js` branches on `result.updated` to print `"Updated existing review comment on ..."` vs. `"Posted review comment to ..."`; 2 existing assertions updated and 1 new regression test added (mocking `postReviewComment` with `{ updated: true }`, the shape the pre-existing tests never used). Suite is 227/227 green in `reviewer/` (226 pre-existing + 1 net-new), 29/29 in `sample-app/` (unaffected). Also updated `.claude/skills/review-pr/SKILL.md` and its byte-identical `plugin/skills/review-pr/SKILL.md` copy to document both possible success messages, logged here under the same "skills" self-modification category as the task-16 entry below.

## 2026-07-18 (task 16: update-in-place PR comment posting)
**Change:** `reviewer/src/github/postReviewComment.js` now searches the PR's existing comments for one it posted previously (identified by a hidden `BOT_COMMENT_MARKER` prefix) and `PATCH`es it in place instead of always `POST`ing a new comment. Updated `.claude/skills/review-pr/SKILL.md` and its byte-identical `plugin/skills/review-pr/SKILL.md` copy to document this behavior change.

**Evidence:** TASKS.md item 16, proposed in RETRO.md's 2026-07-18 self-audit, citing PROGRESS.md's task-5 entry (which first raised "should this update an existing comment instead of always posting a new one?" as an open question) and task-9's entry (which restated it, unresolved, when the `review-pr` skill — explicitly designed for repeat invocation against the same PR — shipped). This is TASKS.md backlog work, not a discretionary self-improvement, but logged here per hard invariant 3, which names "skills" as one of the three self-modification categories requiring a changelog entry regardless of discretion — same precedent as the task-9/task-11/task-14 entries below. Without this fix, reviewing the same PR more than once (the skill's own stated normal use case) grows an ever-longer comment thread instead of keeping one up-to-date review comment. Suite is 226/226 green in `reviewer/` (221 pre-existing + 5 net-new regression tests covering the search/PATCH/POST-fallback/pagination/marker-position behavior), 29/29 in `sample-app/` (unaffected).

## 2026-07-18 (adversarial bug-hunt run, post-task-14)
**Change:** Added a new guideline to CLAUDE.md (below the hard invariants) requiring a rule's exemption/allowlist condition to be checked against the same specific value its positive check already matched, never against the whole line/block the positive check happened to be scanning — and requiring, before trusting such an exemption, a check of what the specific case it claims to protect would do if the exemption were deleted outright.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-14" entry — `reviewer/src/rules/security.js`'s hardcoded-credential check exempted a match whenever the literal string `process.env` appeared *anywhere* on the line, not just as the assigned value. `SECRET_PATTERN` only matches when an operator (`:`/`=`) is immediately followed by a quoted literal, which a genuine `= process.env.X` read never is, so the exemption never actually protected the one case its own pre-existing test covered (`does not flag secrets read from process.env` passed identically with the exemption deleted). What it did do instead was create a bypass: reproduced directly, `const password = "hunter2"; // fallback for process.env.PASSWORD` and `const password = "abc123" || process.env.PASSWORD;` both produced zero issues before the fix, silently letting a real hardcoded secret through whenever `process.env` was mentioned anywhere else on the same line — a trailing comment or an `||` fallback expression, not the assignment itself. This is a new bug shape for this repo's most-documented recurring class (the five aggregation-scoping entries above): those were all *positive* checks reasoning over too broad a scope; this is a *negative*/exemption check making the same mistake in mirror image. Fixed in the same run (deleted the `ENV_PATTERN`/whole-line guard entirely — no replacement was needed, since `SECRET_PATTERN`'s own structure already makes the case it claimed to protect unreachable; 1 new regression test in `tests/rules/security.test.js`). Suite is 215/215 green in `reviewer/` (214 pre-existing + 1 net-new), 29/29 in `sample-app/` (unaffected).

## 2026-07-18 (task 14: package doc-drift into plugin/, generalize plugin.test.js)
**Change:** Added `plugin/skills/doc-drift/SKILL.md` (byte-identical copy of `.claude/skills/doc-drift/SKILL.md`), and rewrote `reviewer/tests/plugin.test.js`'s skill-parity checks to enumerate every directory under `.claude/skills/` at test-run time instead of a hardcoded `['review-pr', 'standup']` array. Also updated `plugin/.claude-plugin/plugin.json`, `marketplace.json`, and the README's plugin section to describe all three packaged skills.

**Evidence:** PROGRESS.md, 2026-07-18 "task 14" entry, and the post-task-12/task-13 entries it builds on — the post-task-12 PROGRESS.md entry explicitly raised "should `doc-drift` be added to `plugin/`?" as unresolved, and task 13's self-audit (RETRO.md, promoted to TASKS.md item 14) confirmed directly that `plugin/skills/` still only contained `review-pr`/`standup` a full task-cycle after `doc-drift` (task 12) shipped — a real, observed packaging-parity gap, not a speculative one. This is TASKS.md backlog task 14 itself (planned work, not a discretionary improvement), but logged here per hard invariant 3, which names "skills" as one of the three self-modification categories requiring a changelog entry regardless of discretion — same rationale the task-11 entry used for the first plugin packaging. The hardcoded `skillNames` list in `plugin.test.js` was the same latent gap in miniature: it silently wouldn't have caught `doc-drift`'s continued absence from `plugin/` even after this fix, or any future skill's, without a code change to the test itself every time — generalizing it to enumerate `.claude/skills/*` closes that off structurally, the same "fix the class, not the instance" approach the CLAUDE.md aggregation-scoping guideline (see the five entries above) already established for `reviewer/src/rules/*`. Suite is 214/214 green in `reviewer/` (210 pre-existing + 4 net-new: doc-drift's own existence/frontmatter/byte-identical-copy cases plus a guard test asserting the enumeration finds at least one skill), 29/29 in `sample-app/` (unaffected). Verified with `claude plugin validate ./plugin --strict` — passed.

## 2026-07-18 (adversarial bug-hunt run, post-task-13)

**Change:** Refined the CLAUDE.md aggregation/block-scoping guideline (below the hard invariants) for a fifth time. Removed "the next occurrence's own start" from the list of valid ways to derive a per-occurrence block's boundary, and replaced it with an explicit warning that it is unsafe as a boundary unless something already guarantees no unrelated content can sit between two occurrences of the same construct — which is not the normal case in real code. Also widened the required test coverage from "an adjacent-independent-occurrences case" to "an adjacent-independent-occurrences case with unrelated, non-occurrence content in between," since a test with nothing but whitespace between two occurrences cannot catch this shape.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-13" entry — `reviewer/src/rules/errorHandling.js`'s async-handler check bounded each handler's block as "this handler's start index to the next handler's start index (or end of file)," which is exactly the boundary derivation the pre-existing CLAUDE.md guideline explicitly listed as acceptable ("...or the next occurrence's own start"). Reproduced directly: a diff adding `getWidget` (no try/catch), then an unrelated `function unrelatedHelper() { try {...} catch {...} }`, then `deleteWidget` (no try/catch), produced only 1 issue (for `deleteWidget`) instead of 2 — `unrelatedHelper`'s own unrelated try/catch was swept into `getWidget`'s window (bounded by "up to the next handler's start," i.e. `unrelatedHelper` is not a handler so scanning continued past it to `deleteWidget`'s start) and silenced a genuine finding for a handler with no error handling of its own. This is the fifth recurrence of the guideline's underlying bug class (see the four prior entries below and in CLAUDE.md's own evidence trail), but the first recurrence caused by *following* the guideline's own stated exception rather than by a rule author missing the guideline entirely — meaning the guideline itself, not just vigilance in applying it, needed correcting. Fixed in the same run (`collectHandlerBlock` now brace-depth-bounds each handler by its own `{`...`}`, mirroring `collectForEachBlock`/`collectThenStatement`'s existing technique in sibling files, instead of using the next handler's position; 1 new regression test in `tests/rules/errorHandling.test.js`, verified to fail against the pre-fix "next handler's start" boundary and pass against the fix). Suite is 210/210 green in `reviewer/` (199 pre-existing + 11 net-new regression tests from this run's 8 bug fixes, including this one), 29/29 in `sample-app/` (unaffected) — this changelog entry codifies the corrected guideline so a sixth recurrence via the same now-removed exception doesn't ship.

## 2026-07-18 (task 13: self-audit, RETRO.md, backlog additions)
**Change:** Added TASKS.md items 14–16 (backlog order/content change): package `doc-drift` into `plugin/`; extract a shared `collectBoundedBlock` helper for `errorHandling.js`/`performance.js`; make `review-pr`'s PR-comment posting update an existing bot comment in place instead of always creating a new one. Marked task 13 `[DONE]`.

**Evidence:** RETRO.md (this run) synthesizes evidence already on record in PROGRESS.md/this file rather than introducing anything new: (14) the post-task-12 PROGRESS.md entry explicitly raised "should doc-drift be added to plugin/" as unresolved, and this run confirmed directly that `plugin/skills/` still only contains `review-pr`/`standup` — `doc-drift` (task 12) was never added. (15) The "scope a check to its local occurrence, never aggregate across a whole file/diff" bug class shipped four separate times in four shapes — 2026-07-17 reviewer adversarial-hunt run (`errorHandling.js` file-wide booleans), post-task-3 (`missingTests.js` cross-file boolean), post-task-7 (`performance.js` joined-string regex, missed by a prior audit that specifically checked for this class and cleared it), and post-task-12 (`errorHandling.js`'s fixed-3-line-window `.then()`/`.catch()` check) — and the post-task-12 entry itself named the shared-helper refactor as the structural fix but left it undone pending "an observed failure" that had by then already recurred four times; extracting the helper is proposed so future rules get correct scoping by construction rather than re-deriving their own boundary heuristic each time. (16) "Update an existing bot comment instead of always posting a new one" was raised as an open question in the task-5 PROGRESS.md entry and restated verbatim in the task-9 entry, never resolved since; task 9's own `review-pr` skill is explicitly designed for repeat invocation against the same PR, which makes the current one-comment-per-run behavior a concrete, observed-shape usability problem (a growing comment thread on any PR reviewed more than once), not a speculative one. Deliberately did *not* add tasks for `collectPullRequests` pagination past 50 or standup markdown-escaping — both raised repeatedly in prior PROGRESS.md entries but with no run yet observing a repo/sink that actually triggers either, so left as documented-but-deferred in RETRO.md rather than added to the backlog. Suite unaffected (docs/backlog-only change): `reviewer/` 199/199 green, `sample-app/` 29/29 green, both re-verified after this change.

## 2026-07-18 (adversarial bug-hunt run, post-task-12)
**Change:** Broadened the existing CLAUDE.md guideline on diff-scanning static-analysis rules (below the hard invariants) to explicitly cover a fourth shape of the same underlying violation: a per-occurrence block that *is* correctly extracted (not file-wide, not a joined blob) but whose boundary is an arbitrary fixed-size line window instead of one derived from the code's actual structure. Added a requirement that such a rule's tests include both a multi-line-formatted-but-compliant case and an adjacent-independent-occurrences case.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-12" entry — `reviewer/src/rules/errorHandling.js`'s `.then()`/`.catch()` check sliced a fixed 3-line window after each `.then(` call and tested it as one joined string. Reproduced directly, both shapes: a legitimately `.catch()`-terminated chain formatted one `.then()` per line (5 lines total) was misflagged as uncaught because its real `.catch()` fell outside the 3-line window (false positive — `errorHandlingRule.check(...)` returned an issue for a chain that IS caught); and two adjacent, independent statements — `jobA.then(doA);` immediately followed by `jobB.then(doB).catch(handleB);` — produced zero issues, because `jobB`'s unrelated `.catch()` fell inside the window opened by `jobA`'s uncaught `.then()` (false negative — reproduced with `errorHandlingRule.check(...)` returning `[]` when it should have flagged `jobA`'s line). This is the same "local block/occurrence, not an aggregate" principle the existing guideline already codifies (three prior recurrences: `errorHandling.js`'s file-wide booleans, `missingTests.js`'s cross-file boolean, `performance.js`'s joined-blob regex) but in a new shape none of those three catches: the block WAS being extracted per-occurrence here, just with the wrong-sized boundary. None of the five pre-existing tests for this specific check used a chain longer than 3 lines or two adjacent independent `.then()` statements, so this shipped undetected since the check was first added (task 2, `errorHandling adversarial-hunt run` fixed only the sibling try/catch-scoping half of this same file, not the `.then()`/`.catch()` half). Fixed in the same run (`collectThenStatement` in `errorHandling.js` now walks forward tracking paren depth, stopping at a paren-balanced `;`-terminated line or the first following line that isn't a `.`-chain continuation, rather than a fixed 3-line slice; 2 new regression tests — one per false-positive/false-negative shape — verified to fail against the pre-fix window and pass against the fix). Suite is now 199/199 green in `reviewer/` (192 pre-existing + 2 net-new `errorHandling` regressions + 1 net-new `parseExpressRoutes` OPTIONS/HEAD regression, both fixed in the same run alongside this changelog entry's own fix + 4 net-new `skillsFrontmatter.test.js` cases), 29/29 in `sample-app/` (unaffected) — this changelog entry codifies the broadened guideline so a fifth recurrence (a fixed-size window used anywhere else a future rule needs per-occurrence scoping) is caught by the same principle going forward.

## 2026-07-18 (adversarial bug-hunt run, post-task-11)
**Change:** Added a guideline to CLAUDE.md (below the hard invariants) requiring report/message-formatting functions to build any wording describing a dynamic input (time window, count, scope) from the actual value given, never a hardcoded phrase for only the common/default case — including in "no results"/empty-state branches — and requiring a test case where the dynamic input differs from the default value, not only a no-argument/default case.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-11" entry — `reviewer/src/standup/formatStandup.js`'s empty-groups branch unconditionally rendered `'No commits or pull requests in the last 24h.'` regardless of the actual queried window. `standupCli.js` accepts an arbitrary `--since <iso-date>` flag (task 10), and `generateStandup.js` always resolves it to a concrete `Date` and passes its ISO string through as `meta.since`. Reproduced directly: `formatStandupReport(new Map(), { since: '2020-01-01T00:00:00.000Z' })` rendered `**Since:** 2020-01-01T00:00:00.000Z` immediately followed by `No commits or pull requests in the last 24h.` — two adjacent lines directly contradicting each other (a ~6-year window described as "the last 24h"). This is a new bug class — not aggregation, not an unguarded pipeline stage, not a missing/asymmetric `await`, not a timezone-unsafe sort, not unencoded URL interpolation, not a silent-`undefined` CLI flag, not invalid YAML frontmatter — so it's captured as its own entry. The existing test for this branch called `formatStandupReport(new Map())` with no `meta` argument at all, so it never exercised the exact combination that breaks and passed despite the bug. Fixed in the same run (message now reads `` `No commits or pull requests since ${meta.since}.` `` when `meta.since` is given, falling back to the old "last 24h" wording only when it's genuinely absent; updated the existing empty-map test to cover the no-meta fallback specifically, added a new test for the `since`-aware wording, and updated `generateStandup.test.js`'s equivalent assertion since `generateStandup` always populates `meta.since` even in its default-24h-ago case, so the more precise wording is correct there too — not just for explicit `--since` calls). Suite 192/192 green in `reviewer/` (191 pre-existing + 1 net new), 29/29 in `sample-app/` (unaffected) — this changelog entry codifies the guideline so future report-formatting code (task 12's doc-drift skill, task 13's self-audit) doesn't reintroduce a hardcoded-assumption message alongside dynamic context that can contradict it.

## 2026-07-18 (task 11: package skills as a Claude Code plugin)
**Change (packaging):** Added `plugin/` — a `.claude-plugin/plugin.json` manifest, a self-referencing `.claude-plugin/marketplace.json` for local persistent install, and `plugin/skills/review-pr/SKILL.md` + `plugin/skills/standup/SKILL.md` (copies of the existing `.claude/skills/*` files, since an installed plugin can't reference files outside its own directory). This is TASKS.md backlog task 11 itself — the explicit next planned deliverable, not a discretionary self-improvement — but logged here anyway per hard invariant 3, which names "skills" as one of the three self-modification categories requiring a changelog entry, matching the same rationale the task-9 entry below used for the first `.claude/skills/` file. See PROGRESS.md's 2026-07-18 "task 11" entry for the full design (manifest field choices, why copies over symlinks, the marketplace naming) and test record (`reviewer/tests/plugin.test.js`, validated with `claude plugin validate`).

**Change (bug fix):** Fixed invalid YAML in the `argument-hint` frontmatter field of both existing skill files — `.claude/skills/review-pr/SKILL.md` and `.claude/skills/standup/SKILL.md` (and their new `plugin/skills/*` copies, added by this same run) — by quoting the value. Also added `reviewer/tests/plugin.test.js`, which parses every shipped `SKILL.md`'s frontmatter as YAML and fails if it doesn't parse or is missing `name`/`description`.

**Evidence:** PROGRESS.md, 2026-07-18 "task 11" entry — packaging the two skills into `plugin/` (task 11) and running `claude plugin validate ./plugin --strict` (the first time anything in this repo actually validated a `SKILL.md`'s frontmatter) surfaced that `argument-hint: [owner] [gh-repo] [pr-number] [base..head]` and `argument-hint: [owner] [gh-repo] [since]` are invalid YAML: a value starting with `[` opens a flow sequence, and the trailing space-separated `[...]` groups are leftover tokens the parser can't place, so the whole frontmatter block fails to parse. The CLI's own error text confirms the runtime consequence: "At runtime this skill loads with empty metadata (all frontmatter fields silently dropped)" — meaning `name`, `description`, and `argument-hint` were all silently discarded for both skills, not just the one malformed field. This bug predates this run (it shipped in tasks 9 and 10, 2026-07-18 earlier the same day) and was invisible until now because nothing in `reviewer/`'s Jest suite ever parsed a `SKILL.md`'s frontmatter — the skills were exercised only by manually reading and following their prose instructions, which doesn't touch the YAML parser. Fixed by quoting the value in all four files; confirmed the new test catches a regression by reverting the fix in `plugin/skills/review-pr/SKILL.md` alone, rerunning `reviewer/tests/plugin.test.js` (2 failures: the frontmatter-parse case and the byte-identical-to-standalone-copy case), then restoring the fix and rerunning to green. Suite 191/191 green in `reviewer/` (181 pre-existing + 10 new), 29/29 in `sample-app/` (unaffected). This is a new bug class for this repo — invalid/silently-dropped YAML frontmatter in a skill file, not any of the five existing diff-scanning-rule or CLI-parsing classes already logged above — so it's captured as its own entry, and the new test generalizes to catch it in any future `SKILL.md` this repo or the plugin ships, not just these two.

## 2026-07-18 (adversarial bug-hunt run, post-task-10)
**Change:** Added a guideline to CLAUDE.md (below the hard invariants) requiring a CLI parser's value-taking flag to throw a clear "Missing value for --x" error when it's the last token in `argv` (no following value), never silently assign `undefined` and continue — particularly important for optional flags, where a silently-`undefined` value is indistinguishable from the flag never being given.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-10" entry — `reviewer/src/standupCli.js`'s `parseStandupArgs` (task 10, new this run) let `--owner` reach `undefined` when given as the last CLI token, and its owner/gh-repo pairing check only tested truthiness (`args.owner && !args.ghRepo`), so `undefined` passed the check exactly like "flag never given." Reproduced directly: `parseStandupArgs(['--repo', '.', '--owner'])` returned `{ owner: undefined, ghRepo: null, ... }` with no thrown error, and the CLI would silently generate a commits-only report with zero indication `--owner` had been typed at all. The identical shape — reproduced, not just theorized — also existed in the three older parsers (`cli.js`, `reviewPrCli.js`, `docDriftCli.js`) for their own optional value-taking flags (`--out` in particular silently fell back to printing to stdout instead of writing the file the caller asked for). This is a new bug class — not URL encoding, not aggregation, not an unguarded pipeline stage, not a missing `await`, not a timestamp sort — so it's captured as its own entry. Fixed in the same run in all four parsers (each flag lookup now throws `Missing value for --<flag>.` when the next token is `undefined`; 5 new regression tests, one per parser plus a second case in `standupCli.test.js`; suite 181/181 green in `reviewer/`, 29/29 in `sample-app/`) — this changelog entry codifies the guideline so future CLI entry points (e.g. task 11's plugin packaging, task 12's doc-drift skill) don't reintroduce the same silent-fallback shape for their own optional flags.

## 2026-07-18 (adversarial bug-hunt run, post-task-9)
**Change:** Added a guideline to CLAUDE.md (below the hard invariants) requiring any caller-supplied value (CLI flag, etc.) that's interpolated into a URL/path to be `encodeURIComponent()`-ed at the point of interpolation, never spliced in raw, and requiring a `/`-containing-value test case for any such URL-building function.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-9" entry — `reviewer/src/github/postReviewComment.js` built its GitHub API request URL by directly interpolating `owner`, `repo`, and `prNumber` (sourced from `reviewPrCli.js`'s `--owner`/`--gh-repo`/`--pr` flags) into a template string with no encoding. Reproduced directly: an `owner` value containing `/` (e.g. `attacker/other-owner`) silently redirected the POST request to a different path under `api.github.com` than the one reported back to the user as success, while still attaching the real bearer token in the request. This is a new bug class — not file/cross-file aggregation, not an unguarded pipeline stage, not a timestamp-sort or missing-await issue — so it's captured as its own entry, distinct from the four existing guidelines. Fixed in the same run (`encodeURIComponent()` on all three interpolated values in `postReviewComment.js`, plus a `--pr` numeric-format guard in `reviewPrCli.js`'s `parseReviewPrArgs` as defense-in-depth; 2 new regression tests — one asserting URL-encoding in `tests/github/postReviewComment.test.js`, one asserting the `--pr` format guard in `tests/reviewPrCli.test.js`; suite 161/161 green in `reviewer/` before the same run's second, separately-logged docDrift bug fix brought it to 162/162). This changelog entry codifies the guideline so future GitHub-API-calling code (tasks 10-13's planned skills) doesn't reintroduce the same unencoded-interpolation shape.

## 2026-07-18 (task 9: review-pr skill)
**Change:** Added `.claude/skills/review-pr/SKILL.md` (61 lines, under the 150-line cap) and its underlying script `reviewer/src/reviewPrCli.js`, which composes the existing `git diff` → `loadRules` → `reviewDiff` → `formatMarkdownReport` pipeline (from `reviewer/src/cli.js`) with `postReviewComment` (from task 5) to review a PR's `base..head` range and post the report as a PR comment, with a `--no-post` dry-run escape hatch and an explicit `--token` override alongside `GITHUB_TOKEN` env sourcing.

**Evidence/justification:** This is not a discretionary self-improvement subject to the "cite an observed failure" bar in the paragraph below the hard invariants — it is TASKS.md backlog task 9 itself, the explicit next planned deliverable, done in full per this run's instructions. Logged here anyway because hard invariant 3 names "skills" as one of the three self-modification categories requiring a changelog entry, and this is the first file ever created under `.claude/skills/`. Frontmatter and structure (YAML `description`/`name`/`argument-hint`, supporting-file references, concise task-content body) follow the official Claude Code skills docs (`code.claude.com/docs/en/skills`), fetched and read in this run rather than assumed from memory. See PROGRESS.md's 2026-07-18 "task 9" entry for the design decisions (flag naming, dry-run behavior, token sourcing) and the full test/verification record.

## 2026-07-18 (adversarial bug-hunt run, post-task-8)
**Change:** Added a guideline to CLAUDE.md (below the hard invariants) requiring every dependency-injected collaborator of the same shape/role in a multi-collaborator function to be `await`ed consistently — never awaiting some and not others just because today's default implementation for the un-awaited one happens to be synchronous — and requiring an async-mock test case for any such "presumably sync" collaborator.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-8" entry — `reviewer/src/standup/generateStandup.js` awaited `collectPullRequestsFn(...)` (line 22) but called `collectCommitsFn(...)` without `await` (line 21), even though both are injected via the same `deps` object and `collectCommits`'s real (non-test) implementation is exported from the same module family as the awaited `collectPullRequests`. Reproduced directly: injecting an async `collectCommits` mock (a natural thing to do, since its sibling collaborator is already async) left `commits` as an unresolved Promise, which crashed `groupByAuthor`'s `for (const commit of commits)` with "commits is not iterable". This is a new bug class — not file-wide aggregation, not an unguarded CLI stage, not a string-vs-numeric timestamp sort — so it's captured as its own entry. Fixed in the same run (added the missing `await`, 1 new regression test in `tests/standup/generateStandup.test.js` using an async `collectCommits` mock, verified to fail against the pre-fix code and pass against the fix; suite 139/139 green in `reviewer/`, 29/29 in `sample-app/`).

## 2026-07-18 (adversarial bug-hunt run, post-task-7)
**Change:** Broadened the existing CLAUDE.md file-wide-aggregation guideline (below the hard invariants) to explicitly cover a second shape of the same violation: a single regex tested against a joined/combined multi-line string of all added lines, not just a `hasX` boolean computed once and reused. Also added an explicit audit instruction to check for `join('\n')`/combined-string regex tests specifically when reviewing a rule for this bug class.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-7" entry — `reviewer/src/rules/performance.js`'s `AWAIT_IN_FOREACH` check tested a single non-brace-aware regex against `combinedAdded` (all of a file's added lines joined into one string), rather than scoping to each individual `.forEach()` callback's own body. This produced both a false positive (a completely clean `.forEach()` with no `await` inside it was misflagged because an unrelated `await` appeared later in the file inside some other block that happened to close with `}` `)`) and a misattribution bug (a genuinely bad `.forEach()` following a clean one had its finding's line number anchored to the clean one instead). This is the same underlying class the existing guideline (line 14, written for `errorHandling.js`, already broadened once for `missingTests.js`'s cross-file recurrence) exists to prevent — but in a shape that a prior adversarial-hunt audit of this exact file explicitly missed: the 2026-07-17 "reviewer adversarial-hunt run" PROGRESS.md entry states `performance.js` was checked against this false-negative shape "and found no analogous bug," which was wrong because that audit looked for the reused-boolean shape and never noticed `AWAIT_IN_FOREACH` was testing a joined-string blob (unlike the file's other check, `DEEP_CLONE_ANTIPATTERN`, which correctly iterates per-line). Fixed in the same run (brace-depth block extraction per `.forEach()` occurrence in `performance.js`, mirroring `errorHandling.js`'s existing block-extraction approach; 2 new regression tests; suite 138/138 green in `reviewer/`, 29/29 in `sample-app/`) — this changelog entry codifies the broadened guideline wording, plus the explicit "check for joined-string regexes, not just reused booleans" audit instruction, so a fourth occurrence of this class isn't missed the way this third one was.

## 2026-07-18 (adversarial bug-hunt run, post-task-6)
**Change:** Added a guideline to CLAUDE.md (below the hard invariants) requiring timezone-offset-preserving timestamp strings (e.g. git's `%aI`) to be parsed to `Date`/epoch and compared numerically, never sorted via plain string comparison, and requiring a mixed-offset test case for any such sort.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-6" entry — `reviewer/src/standup/formatStandup.js` sorted each author's commits with `a.date.localeCompare(b.date)` on the `%aI` author-date string, which preserves the commit's original author-timezone offset rather than normalizing to UTC. Confirmed a real misordering: a commit dated `2026-07-18T23:30:00-07:00` (`2026-07-19T06:30:00Z` — later in real time) sorted *before* one dated `2026-07-19T01:00:00+00:00` (`2026-07-19T01:00:00Z` — earlier), purely because `'07-18' < '07-19'` as text. This is a new bug class — not the file/cross-file aggregation class the two existing rule-related guidelines cover, and not the unguarded-pipeline-stage class the CLI guideline covers — so it's captured as its own entry. All four pre-existing sort tests used `Z`-only or matching-offset fixtures, which happen to sort correctly as text, so the bug shipped undetected; a first attempt at a regression test also passed against the buggy code for the same reason (offsets that didn't straddle a day boundary), which is why the guideline explicitly calls out that same-offset/coincidental-pass risk. Fixed in the same run (`new Date(a.date) - new Date(b.date)` in `formatStandup.js`, 1 new regression test using offsets that straddle a UTC day boundary, verified to fail against the pre-fix code and pass against the fix; suite 95/95 green in `reviewer/`, 29/29 in `sample-app/`).

## 2026-07-18 (adversarial bug-hunt run, post-task-5)
**Change:** Added a guideline to CLAUDE.md (below the hard invariants) requiring a script's testable entry point (e.g. `runCli`) to wrap every pipeline stage in try/catch, not only the stages already anticipated to fail, and requiring an unexpected-throw test case for every stage.

**Evidence:** PROGRESS.md, 2026-07-18 "adversarial bug-hunt run, post-task-5" entry — `reviewer/src/cli.js`'s `runCli` guarded `parseArgs`, `execGit`, and `loadRulesFn` with try/catch (each returning exit code 1 with a clear stderr message on failure), but left `reviewDiffFn`, `formatReport`, and `writeFile` completely unguarded. An exception from any of those three — reproduced directly by making `writeFile` throw (e.g. `--out` pointing at a nonexistent directory) — propagated as an uncaught exception and crashed the process with a raw stack trace instead of the controlled exit-code-1 behavior every other failure path in the same function already had. This is a new bug class (inconsistent error-handling coverage across a single function's pipeline stages), distinct from the two aggregation-scope guidelines already in CLAUDE.md, so it's captured as its own entry. Fixed in the same run (both stages now wrapped in try/catch, 2 new regression tests in `tests/cli.test.js`, suite 71/71 green) — this changelog entry codifies the guideline so future CLI/script entry points in this repo don't ship with partial error-handling coverage.

## 2026-07-17 (adversarial bug-hunt run, post-task-3)
**Change:** Broadened the existing CLAUDE.md guideline on diff-scanning static-analysis rules (below the hard invariants) to explicitly cover cross-file aggregation, not just within-a-single-file aggregation. Also updated its required test coverage to include a "2+ source files in one diff, mixed compliant/non-compliant" case, not only a "2+ occurrences in one file" case.

**Evidence:** PROGRESS.md, 2026-07-17 "adversarial bug-hunt run, post-task-3" entry — `reviewer/src/rules/missingTests.js` computed `testFiles.length > 0` once for the entire diff and reused that single boolean for every source file, so a test file changed for one source file silenced the missing-tests finding for a completely different, untested source file in the same diff. This is the same underlying bug class as the `errorHandling.js` file-wide-aggregation bug the existing CLAUDE.md guideline was written to prevent (2026-07-17 "reviewer adversarial-hunt run" entry) — except at cross-file scope rather than within-file scope. The guideline's original wording ("never aggregate a single boolean across a whole file diff") didn't explicitly rule out cross-file aggregation, so it didn't prevent this second occurrence of the same class of false negative from shipping in task 2/3. Fixed in the same run (basename-based per-source-file test matching in `missingTests.js`, 1 new fixture, 1 new regression test, suite 47/47 green) — this changelog entry codifies the broadened scope so a third occurrence (e.g. aggregating across multiple hunks, or across multiple rule invocations) is caught by the same guideline going forward.

## 2026-07-17
**Change:** Added a guideline to CLAUDE.md (below the hard invariants) requiring async Express route handlers to be wrapped in error handling (routed to a catch-all error middleware) and requiring every handler's test suite to include at least one rejected-promise test case, not only resolved-happy-path mocks.

**Evidence:** PROGRESS.md, 2026-07-17 "adversarial bug-hunt run" entry — `usersController.js` and `tasksController.js` had zero error handling around their async Mongoose calls, and `app.js` had no catch-all error middleware. A rejected promise (e.g. a real Mongoose `CastError` from an invalid ObjectId in the URL, or a transient DB failure) hung the HTTP request indefinitely instead of returning an error response — a DoS-shaped bug. It shipped to `claude/dev` undetected because all 25 existing tests only ever mocked resolved values; none exercised a rejection path. The bug itself was fixed in the same run (new `src/middleware/asyncHandler.js`, wired into both route files, plus a catch-all error middleware in `app.js`, plus 4 new regression tests), but the underlying cause — test suites that only cover the happy path — is a repo-wide risk worth codifying so it isn't reintroduced by later tasks (e.g. task 2's reviewer core).

## 2026-07-17 (reviewer adversarial-hunt run)
**Change:** Added a guideline to CLAUDE.md (below the hard invariants) requiring diff-scanning static-analysis rules (`reviewer/src/rules/*`, and later `review-rules.yaml`) to scope pattern checks to the local block/occurrence rather than aggregating a single boolean across the whole file diff, and requiring every such rule's tests to include a multi-occurrence (mixed compliant/non-compliant) case.

**Evidence:** PROGRESS.md, 2026-07-17 "reviewer adversarial-hunt run" entry — `reviewer/src/rules/errorHandling.js` computed `hasTry`/`hasAsyncWrapper`/`hasCatch` once for an entire file's added lines and reused those booleans for every async handler and every `.then()` call in that file. A diff adding two async handlers to one file — one wrapped in try/catch, one not — produced zero issues, because the first handler's try/catch satisfied the file-wide flag and silenced the check for the second, genuinely unguarded handler. The same false-negative shape hit `.then()`/`.catch()` detection. This defeats the rule's purpose in any file with more than one handler (the norm for real controllers, as sample-app's own files demonstrate), and none of the 4 pre-existing tests exercised more than one handler/chain per file, so it shipped undetected on `claude/dev`. Fixed in the same run (block-scoped checks in `errorHandling.js`, 2 new regression tests, 1 new fixture) — this changelog entry codifies the underlying test-coverage gap so the same class of bug isn't reintroduced when task 3 (`review-rules.yaml`) or later rules are added.
