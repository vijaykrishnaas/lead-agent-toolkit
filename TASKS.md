# Backlog. Work top to bottom unless reordering is logged in PROGRESS.md with rationale.
# Mark [DONE] or [BLOCKED: reason]. Never delete tasks — only add or reorder.
1. [DONE] Scaffold sample-app/: minimal Express+Mongo REST API (users, auth, tasks CRUD), Jest, plain JS. `npm test` green.
2. [DONE] Reviewer core: diff -> structured review JSON (risk, issues[]: file/line/category/fix). Categories: security, missing-tests, error-handling, performance, style. Fixture-based unit tests.
3. [DONE] review-rules.yaml config with MERN defaults; loader + tests.
4. [DONE] CLI: `npm run review -- <base>..<head>` -> markdown report. Verify against sample-app history.
5. [DONE] GitHub integration: post review as PR comment (token via env, mocked in tests).
6. [DONE] Standup module: last 24h commits/PRs -> per-author standup markdown.
7. [DONE] Doc-drift module: Express routes vs openapi.yaml -> mismatch report.
8. [DONE] README + mermaid architecture diagram.
9. [DONE] Skill: .claude/skills/review-pr/SKILL.md — runs the reviewer on a PR and posts results. Follow Claude Code skill-authoring best practices (check official docs in-run).
10. [DONE] Skill: .claude/skills/standup/SKILL.md — generates standup digest.
11. [DONE] Package skills as an installable Claude Code plugin (manifest per current plugin docs) so Vijay can install locally: `plugin/` dir + install instructions in README.
12. [DONE] Skill: .claude/skills/doc-drift/SKILL.md.
13. [DONE] Self-audit: read all PROGRESS.md entries, write RETRO.md — what failed, what to improve, propose 3 new backlog tasks.
14. [DONE] Package `doc-drift` into `plugin/` for parity with `review-pr`/`standup`; generalize `plugin.test.js`'s copy-drift guard to enumerate `.claude/skills/*` instead of a hardcoded list. (Proposed in RETRO.md, 2026-07-18 self-audit.)
15. [DONE] Extract a shared `collectBoundedBlock`-style helper into `reviewer/src/utils/` and refactor `errorHandling.js`/`performance.js` to use it for per-occurrence block-boundary scoping. (Proposed in RETRO.md, 2026-07-18 self-audit — see SKILL_CHANGELOG.md for the four recurrences of this bug class this closes off structurally.)
16. [DONE] Make `review-pr`'s PR-comment posting update an existing bot comment in place instead of always creating a new one. (Proposed in RETRO.md, 2026-07-18 self-audit — resolves the open question first raised in the task-5 PROGRESS.md entry and restated in task-9's.)
17. [DONE] Harden `sample-app`'s `JWT_SECRET || 'dev-secret'` fallback for production use (throw at startup/first sign-or-verify when `NODE_ENV === 'production'` and `JWT_SECRET` is unset or still the `.env.example` placeholder), in both `authController.js` and `middleware/auth.js`. (Proposed in RETRO.md, 2026-07-19 self-audit round 2 — raised as an open question in the post-task-14, task-16, and post-task-16 PROGRESS.md entries with no new information across three mentions.)
18. [DONE] Add automated regression coverage against doc/prose drift inside this repo's own README.md and `plugin/.claude-plugin/*.json` manifests (e.g. assert `marketplace.json`/`plugin.json` descriptions mention every skill under `plugin/skills/`, generically enumerated like `plugin.test.js`/`skillsFrontmatter.test.js` already do; flag a hardcoded, guaranteed-to-go-stale Jest test count in README). (Proposed in RETRO.md, 2026-07-19 self-audit round 2 — the post-task-14 hunt found three instances of exactly this drift shape in one run, all caught only by manual inspection.)
19. [DONE] Make `findExistingBotComment`'s bot-comment identification resistant to spoofing by a non-owning PR commenter, without relying on a `GET /user` call (breaks the default `GITHUB_TOKEN` credential in GitHub Actions, per the post-task-16 PROGRESS.md entry) or a `comment.user.type === 'Bot'` check (breaks PAT-based local usage, since a human operator's own token posts as `type: 'User'`). Proposed fix: derive `BOT_COMMENT_MARKER`'s suffix per `owner/repo/prNumber` from an HMAC keyed by the GitHub token itself (e.g. `crypto.createHmac('sha256', token).update(`${owner}/${repo}#${prNumber}`).digest('hex').slice(0, 16)`) instead of a fixed public string — unpredictable to anyone without the token, needs no extra API call, and works identically for `GITHUB_TOKEN` and PAT credentials alike. A token rotation between runs would just miss the old comment and post a new one (graceful fallback, same as today's "no prior comment found" path), not a regression. (Proposed in RETRO.md, 2026-07-19 self-audit round 3 — raised in the post-task-16 hunt, re-analyzed with an escalated impact in the following adversarial-hunt run (spoofing can permanently orphan the real bot comment, not just self-limited griefing), and deferred twice for lack of a fix without a compatibility cost; this HMAC-based marker is the first proposed fix that avoids both previously-identified costs.)
20. [DONE] Repo harness + CI (AUDIT.md F4/F12): root package.json whose `npm test` runs both suites (`npm --prefix reviewer test && npm --prefix sample-app test`); GitHub Actions workflow running both suites on Node 20 and 22 on push to claude/dev and main; `engines: ">=20"` in both packages. Makes hard invariant 1 mechanically checkable at the repo root for the first time.
21. [DONE] CLAUDE.md consolidation (AUDIT.md F2 — decided by Vijay 2026-07-20, closing runs 7-8's standing question): keep each guideline below the hard invariants as a compact numbered rule (1-3 lines: rule + required test coverage + one evidence pointer); move the full recurrence narratives VERBATIM (no deletion) to docs/GUIDELINES_HISTORY.md. SKILL_CHANGELOG entry required (invariant 3).
22. [DONE] Formalize empty-backlog behavior in CLAUDE.md (AUDIT.md F1 — closes the 9-times-restated open question): when no non-DONE/non-BLOCKED task exists, in order: (a) work the oldest unresolved open question in PROGRESS.md; (b) else adversarially hunt the LEAST-recently-audited module (not the newest diff — prevents re-mining errorHandling.js); (c) else self-audit into RETRO.md. SKILL_CHANGELOG entry required.
23. [DONE] AST spike (AUDIT.md F3): add acorn to reviewer/; reimplement errorHandling + performance boundary/scoping logic on the AST of the post-image file (resolved via `git show <sha>:<path>` when the CLI has repo access), mapping diff line numbers to AST nodes, behind the existing rule interface; keep the regex path as fallback when file content is unresolvable. Acceptance: every existing rule regression test passes under the AST path. Evidence: ten documented recurrences of the boundary-derivation bug class (CLAUDE.md line 14's own history).
24. [DONE] Restrict `GET /api/users/:id` to self, 403 otherwise (AUDIT.md F7 — decided by Vijay 2026-07-20, closing the ~15-run open question), matching update/remove's existing owner checks; update openapi.yaml + tests (including a cross-user 403 case). Remove the item from the standing open-questions list going forward.
25. [DONE] Doc-drift template-literal route paths (AUDIT.md F8): emit an explicit `unparsed-route` warning finding (file + line, "route exists but path could not be statically resolved") instead of silently skipping; tests for template-literal path and template-literal mount prefix.
26. [DONE] Consolidate findHandlerBraceStart/collectStatement into one shared statement-walk helper (AUDIT.md F9; evidence: run 6's scan-vs-check divergence bug). Mark [DONE: superseded by 23] instead if task 23's AST path has already replaced both callers.
27. [DONE] Plugin sync script (AUDIT.md F10): `npm run sync-plugin` in reviewer/ (or root) copying .claude/skills/* to plugin/skills/; keep the existing byte-parity tests as the guard; document in README that skills are edited only under .claude/skills/.
28. [DONE] Reviewer dogfood CI step (AUDIT.md F13): non-blocking CI job running the reviewer CLI on each push's own diff and uploading the markdown report as an artifact. Depends on task 20's workflow existing.
29. [DONE] Invariant linter in CI (IDEAS.md / AUDIT.md F4 follow-on; evidence: the audit showed invariant 1 was mechanically unenforceable — invariants 2 and 3 have the identical gap, enforced only by agent discipline): CI script failing a push if (a) a diff deletes existing TASKS.md task lines or removes PROGRESS.md/SKILL_CHANGELOG.md content, or (b) CLAUDE.md/.claude/skills/*/TASKS.md changed without a SKILL_CHANGELOG.md addition in the same commit. Depends on task 20's workflow.
30. PROGRESS.md rollover (evidence: file is ~1,140 lines, grows every run, mixed historical ordering — AUDIT.md F11; same reading-tax class as F2): archive entries older than the current month into PROGRESS-<YYYY-MM>.md files (move verbatim, never delete, per invariant 2), keep a header pointer listing archives, current file holds only the active month. SKILL_CHANGELOG entry required.
31. [RW2, pre-authorized — CLAUDE.md "Authorized rewires"] Unify the four hand-rolled CLI parsers into one shared declarative parser module (flag spec: name, takesValue, required, validator), used by cli.js, reviewPrCli.js, standupCli.js, docDriftCli.js. Evidence: the silent-undefined value-flag bug shipped in ALL FOUR parsers independently (PROGRESS.md post-task-10 entry) — per-parser duplication is the recurrence mechanism. All existing parser tests must pass against the shared module.
32. [RW3, pre-authorized — CLAUDE.md "Authorized rewires"] Add a mongodb-memory-server integration-test layer to sample-app alongside the unit mocks (separate jest project or test dir; npm test runs both). Evidence: the duplicate-key race (register 500-vs-409) and tasks owner-scoping bugs were only reachable through real query semantics — the mock-only suites passed while both shipped (PROGRESS.md 2026-07-19 adversarial bug-hunt entry). Cover at minimum: register/login flow, duplicate-email 409, tasks CRUD owner isolation, users/:id self-only 403 (after task 24).
