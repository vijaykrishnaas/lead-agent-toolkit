Plain JavaScript, Node 20, Jest. Branch: claude/dev only.

HARD INVARIANTS — never edit or weaken these three lines:
1. Every commit must pass `npm test`. If you cannot get green this run, revert and mark task BLOCKED.
2. Never delete backlog tasks or PROGRESS/CHANGELOG history.
3. Every self-modification (skills, CLAUDE.md, backlog order) gets a SKILL_CHANGELOG.md entry: date, change, evidence from PROGRESS.md justifying it.

You MAY improve: .claude/skills/*, this file below the invariants, TASKS.md ordering, review-rules.yaml.
Improvements must cite an observed failure or gap — no speculative rewrites. Skills stay under 150 lines each.
Each run: append dated PROGRESS.md entry (done, decisions, open questions for Vijay).

EMPTY-BACKLOG BEHAVIOR (AUDIT.md F1 — closes the 9-times-restated open question; formalized 2026-07-20). When no TASKS.md item is non-DONE and non-BLOCKED, do the following in order and stop at the first that applies:
(a) Work the oldest unresolved open question raised for Vijay in PROGRESS.md (grep every "open question" mention; PROGRESS.md's ordering is mixed per AUDIT.md F11, so pick by earliest date found, not by position in the file).
(b) Else, adversarially hunt the module least recently touched by an adversarial-hunt or task entry in SKILL_CHANGELOG.md/PROGRESS.md — never the newest diff. This is a deliberate reversal of the pre-2026-07-20 default: eleven straight runs re-mined the same ~80-line boundary machinery in `reviewer/src/rules/errorHandling.js` for progressively narrower edge cases (AUDIT.md F1); picking the least-recently-audited module instead spreads coverage across the codebase.
(c) Else, self-audit: read PROGRESS.md, write a dated RETRO.md section (what failed, what to improve), and propose new TASKS.md backlog items with evidence. Do not implement code under this branch — propose only, matching the precedent of the self-audit rounds already in SKILL_CHANGELOG.md.
Always record which branch was taken, and why, in that run's PROGRESS.md entry.

AUTHORIZED REWIRES (granted by Vijay, 2026-07-20 — an explicit, scoped exception to the "no speculative rewrites" bar above; see AUDIT.md and PROGRESS.md 2026-07-20 entries). The four rewrites below are pre-approved: a run picking one up needs no new evidence beyond this section — the hard invariants still apply in full (green suite on every commit, SKILL_CHANGELOG entry, no history deletion). Old SOURCE CODE may be deleted or replaced outright once its replacement's tests pass — invariant 2 protects backlog/history files, never source code. Anything NOT listed here still needs the normal observed-failure evidence.
- RW1 (with task 23): once the acorn AST spike passes every existing rule fixture, fully replace the hand-rolled boundary machinery (`collectBoundedBlock`, `findHandlerBraceStart`, `collectStatement`, the depth-counting consumers of `maskStringLiterals`, `parseExpressRoutes`' chain walking) with the AST path; the regex path survives only as a documented fallback for unresolvable file content. Marks task 26 superseded.
- RW2 (task 31): unify the four hand-rolled CLI parsers (`cli.js`, `reviewPrCli.js`, `standupCli.js`, `docDriftCli.js`) into one shared declarative parser module.
- RW3 (task 32): add a mongodb-memory-server integration-test layer to sample-app alongside (not replacing) the existing unit mocks; controllers/models may be refactored where real-query semantics reveal the mocks were hiding behavior.
- RW4 (enabler for RW1): the rule interface may change from `check(files)` to `check(context)` where `context = { files, resolveFileContent }` — update all rules, `reviewer.js`, and config loaders in one commit.

GUIDELINES (compact; full recurrence narratives — every "why," every reproduction, every prior fix attempt — live verbatim in `docs/GUIDELINES_HISTORY.md`, same numbering. Read the history entry before touching code the guideline covers; this list is a lookup index, not the complete rationale.)
1. Async Express route handlers must never let a rejected promise reach the client unhandled — wrap with an async-error handler and route errors to a catch-all error middleware. Test: every handler suite needs a rejected-promise case. Evidence: PROGRESS.md 2026-07-17.
2. Diff-scanning rules must scope pattern checks to the local block/occurrence, never aggregate across a file or diff; a boundary must be derived from real structure (brace/paren depth, masked of strings/comments), never a fixed window or "next occurrence's start". Test: multi-occurrence case, mixed compliant/non-compliant. Evidence: ten documented recurrences, PROGRESS.md 2026-07-17 through 2026-07-19 — see history for the full chain.
3. A testable entry point (e.g. `runCli`) must wrap every pipeline stage in try/catch, not just the stages anticipated to fail. Test: unexpected-throw case for every stage. Evidence: PROGRESS.md 2026-07-18, `reviewer/src/cli.js`.
4. Timezone-preserving timestamps (e.g. git `%aI`) must be parsed to Date/epoch and compared numerically, never string-compared. Test: mixed-offset fixture. Evidence: PROGRESS.md 2026-07-18, `formatStandup.js`.
5. A function awaiting one of several same-shape DI collaborators must await all of them, even if a collaborator's current default is synchronous. Test: mock the "presumably sync" collaborator as async. Evidence: PROGRESS.md 2026-07-18, `generateStandup.js`.
6. Caller-supplied values interpolated into a URL must be `encodeURIComponent()`-ed at the interpolation site, never spliced raw. Test: value containing `/`. Evidence: PROGRESS.md 2026-07-18, `postReviewComment.js`.
7. A CLI parser's value-taking flag must error loudly ("Missing value for --x") when its value is absent, never silently assign `undefined`. Test: flag as the last token in argv. Evidence: PROGRESS.md 2026-07-18, all four CLI parsers.
8. Report/message wording describing a dynamic input (time window, count, scope) must be built from the actual value, never a hardcoded common-case phrase — including empty-state branches. Test: dynamic input differs from the default. Evidence: PROGRESS.md 2026-07-18, `formatStandup.js` empty-groups branch.
9. A status message describing which of several actions a collaborator took must be derived from that collaborator's actual return value, never hardcoded for one action. Test: collaborator mocked with more than one distinguishable return shape. Evidence: PROGRESS.md 2026-07-18, `reviewPrCli.js`/`postReviewComment`.
10. Depth-counting boundary/keyword checks must mask string/template literals, `//` line comments, and `/* */` block comments before counting or matching. Test: each of the three categories, both an opening- and closing-shaped character. Evidence: PROGRESS.md 2026-07-19, `collectBoundedBlock.js`/`maskStringLiterals.js`.
11. A handler that both validates a precondition and performs a persistent side effect must run every precondition check before committing to the side effect. Test: assert the side-effect collaborator was never called when the precondition fails. Evidence: PROGRESS.md 2026-07-19, `authController.js` register.
12. A rule's exemption/allowlist condition must be checked against the same specific value the positive check matched, never the whole line/block. Test: positive match plus the exemption's trigger text present elsewhere on the same line/block. Evidence: PROGRESS.md 2026-07-18, `security.js` hardcoded-credential check.
13. A "does this text contain/reference X" check must match X as a whole token (word-boundary), never a raw substring. Test: token present only as a substring of an unrelated word/path, real reference absent. Evidence: PROGRESS.md 2026-07-19, `docProseDrift.test.js` and `missingTests.js`.
14. A marker a tool posts then reads back to recognize its own prior content is not a secret once posted — prefer the earliest matching occurrence, never the most recent. Test: two candidates share an identical marker; assert the earlier one is treated as canonical. Evidence: PROGRESS.md 2026-07-19, `findExistingBotComment`.
15. A sanitization fix for attacker-controlled text must be applied to every structurally similar sink, not only the one whose bug prompted the fix — check higher-impact sinks (live-rendered output) especially. Evidence: PROGRESS.md 2026-07-19, `markdownReport.js` vs `formatStandup.js`.
16. An escape-character-based sanitizer must escape the escape character itself, not only the target characters, or pre-existing escape characters in the input flip the parity and leave a target character live. Test: input already containing the escape character before a target character. Evidence: PROGRESS.md 2026-07-19, `sanitizeMarkdownText.js`.
17. A boundary walk's "has this construct ended" continuation check must treat a blank or comment-only line as non-terminating, never as proof the construct ended. Test: comment-only line inserted between two lines of the same construct. Evidence: PROGRESS.md 2026-07-19, `collectStatement`/`handlerHasBraceBody`.
18. A regex that detects whether a code construct exists at all must run against comment-masked (not raw) source, keeping string literal contents intact. Test: the pattern's literal text appears only inside a comment. Evidence: PROGRESS.md 2026-07-19, `parseExpressRoutes.js`.
19. Choosing `maskComments` vs. full `maskStringLiterals` depends on whether the check's regex needs string content intact; this applies at single-line scope too (fresh `quoteState` per line), not only multi-line collected blocks. Evidence: PROGRESS.md 2026-07-19, `style.js` CONSOLE_LOG.
20. A keyword-presence check run against an already-collected multi-line block must test the string/comment-masked version of that text, never the raw text. Test: the required/forbidden marker's literal text appears only inside a comment or string in the block, with no real occurrence elsewhere. Evidence: PROGRESS.md 2026-07-19, `errorHandling.js`/`performance.js`.
21. A recursive tree/graph walk's cycle-guard "visited" tracking must be scoped to the current root-to-node path (fresh copy per call, or add-before/remove-after), never one mutable set shared across the whole traversal. Test: a genuine cycle case and a same-node-reached-via-two-branches case. Evidence: PROGRESS.md 2026-07-20, `collectExpressRoutes.js`.
22. A comment/string-masking fix applied to one pattern-presence check in a rule module must be checked against every other presence check in that same module operating on the same line/block data, not only the check whose false positive prompted the fix. Test: the sibling check's own comment/string false-positive case. Evidence: PROGRESS.md 2026-07-20 (adversarial hunt), `performance.js` — `AWAIT_PATTERN` was masked while sibling checks `FOREACH_START`/`DEEP_CLONE_ANTIPATTERN` in the same function were not.
23. An AST subtree search answering "does the occurrence being checked contain X" (a try/catch, a `.catch()` call, an await) must stop at a nested function's own boundary, never descend into it — a nested function's own try/catch, chained `.catch()`, or await belongs to that inner function's separate execution, not the outer handler/statement/callback being checked. Test: a nested, unrelated function defined and called inside the occurrence, containing X itself, for both a false-negative-prone check (try/catch, catch-call) and a false-positive-prone one (await). Evidence: PROGRESS.md 2026-07-20 (adversarial hunt, Task 23 AST spike review), `errorHandling.js`'s `subtreeHasTryStatement`/`subtreeHasCatchCall` and `performance.js`'s `subtreeHasAwait` — see `reviewer/src/utils/subtreeInOwnScope.js`.
